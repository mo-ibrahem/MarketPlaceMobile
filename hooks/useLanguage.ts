/**
 * useLanguage — convenience hook for accessing the language context.
 *
 * Usage:
 *   const { language, isRTL, changeLanguage } = useLanguage();
 */
export { useLanguage } from '../src/i18n/LanguageContext';
